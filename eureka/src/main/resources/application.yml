
server:
  port: 7001


eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: 127.0.0.1
spring:
  application:
    name: euraka-a